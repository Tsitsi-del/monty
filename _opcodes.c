#include "monty.h"

/**
 * addqu - Function used change node of push to queue
 * @stack: pointer to the head node of stack
 * @num_line: line number
 * Return: NULL
 */

void addqu(stack_t **stack, unsigned int num_line)
{
	(void)stack;
	void(num_line);

	arg.flag = 1;
}

/**
 * addst - Function change node of push to queue
 * @stack: pointer to the head node of stack
 * @num_line: line number
 * Return: NULL
 */

void addst(stack_t **stack, unsigned int num_line)
{
	(void)stack;
	(void)num_line;

	arg.flag = 0;
}

